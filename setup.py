from setuptools import setup, find_packages

setup(
    name='NGG',  # Name of the package
    version='0.1',  # Initial version
    packages=find_packages(),  # Automatically find the packages in your project
)
